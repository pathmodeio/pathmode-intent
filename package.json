{
  "name": "pathmode-intent",
  "version": "0.1.0",
  "private": true,
  "description": "An experimental OpenSpec schema that adds intent.md before the proposal",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "license": "MIT",
  "devDependencies": {
    "js-yaml": "^4.1.0"
  }
}
